"""LLM в исследовательском контуре — ЧАСТЬ II (заглушка).

Два легитимных применения (ТЗ, раздел 21): генерация гипотез (каждая —
в журнал с пометкой источника «llm», генерация без записи запрещена)
и извлечение признаков из текста (только с флагом llm_derived и датой
обучения модели в конфиге).

ГЛАВНАЯ ЛОВУШКА — ретроспективная осведомлённость: LLM «знает», чем
закончились исторические события. Бэктест с LLM-признаками на периоде
до даты обучения модели недействителен; такие признаки валидируются
только вперёд, с момента бумажной торговли.
"""

from __future__ import annotations


def generate_hypotheses(*args, **kwargs):
    raise NotImplementedError("Генерация гипотез LLM — Часть II, после фазы 8.")


def text_to_features(*args, **kwargs):
    raise NotImplementedError("Текст → признак — Часть II, после фазы 8.")
