"""Движок бэктеста — ФАЗА 4 (заглушка).

Дисциплина времени: сигнал по данным на закрытии дня T, исполнение по
открытию дня T+1 с проскальзыванием. Стратегия получает срез данных,
а не весь DataFrame. Обязательный тест на заглядывание в будущее:
сдвиг сигналов на день вперёд должен заметно ухудшать доходность.

Каждый прогон автоматически пишется в журнал гипотез — в обход журнала
прогонов не бывает.
"""

from __future__ import annotations


class BacktestEngine:
    def __init__(self, *args, **kwargs):
        raise NotImplementedError("Движок бэктеста — фаза 4. Модуль ещё не реализован.")
